//Leia vários números positivos até o usuário digitar zero e, para cada número, imprima a sua raiz
//quadrada ou uma mensagem de erro, caso não seja possível calculá-la.

#include <stdio.h>

int main()
{
    int numero, raiz, contador, resto;

    numero = 1;

    while(numero =! 0)
    {
    printf("N: ");
    scanf("%d", &numero);

    if (numero>0)
    {
        
    }

    

    }
}